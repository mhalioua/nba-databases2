<div class="header">
	<h1>Home Page</h1>
</div>

<ul>
	<li><%= link_to @yesterday.date.strftime("%B %e"),  { action: 'game', id: @yesterday.date.strftime("%Y%m%d") } %> </li>
	<li><%= link_to @today.date.strftime("%B %e"),      { action: 'game', id: @today.date.strftime("%Y%m%d") } %> </li>
	<li><%= link_to @tomorrow.date.strftime("%B %e"),   { action: 'game', id: @tomorrow.date.strftime("%Y%m%d") } %> </li>
	<li><%= link_to 'Logout', 					  		{ controller: 'access', action: 'logout' } %> </li>
</ul>